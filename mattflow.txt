For testing just python game functions:

erl

{ok,P} = python:start().
python:call(P,snek,make_fields,[GameName,NodeName]).
python:call(P,snek,add_player,[UserName,UserNode]).
python:call(P,snek,remove_player,[UserName,UserNode]).
python:call(P,snek,get_players,[]).
python:call(P,snek,get_snakes,[]).
python:call(P,snek,get_board,[]).

-----------------------

For testing the erlang functions:

erl -sname somename

c(just_snek_things).

just_snek_things:start_link(gamename).



#### Need to test joining game (using gamename/nodename) as well as move response, etc.

